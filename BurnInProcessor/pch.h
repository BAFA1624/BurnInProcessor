#pragma once

/*
 * pch.h is a precompiled header file.
 * By compiling only once, build performance can be improved for future builds.
 * However, if one file is changed, ALL files are recompiled. So only list
 * infrequently changed files here or the performance advantage is negated.
 */

#include "framework.h"
